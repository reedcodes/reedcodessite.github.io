---
layout: default
---

{%- from "post-meta.njk" import postMeta with context -%}

<h1>{{ title }}</h1>

{%- set postList = collections.posts | reverse -%}

{%- if postList -%}
  <ul>
    {%- for post in postList -%}
      <li>
        {{- postMeta(title=post.data.title, url=post.url) -}}
      </li>
    {%- endfor -%}
  </ul>
{%- endif -%}
